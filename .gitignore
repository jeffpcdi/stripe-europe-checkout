node_modules/
.DS_Store
.env
.env.*
.env*.local
!.env.example

# Build output do Next.js — nunca versionar (é regenerado a cada deploy/build)
.next/

# Estado de runtime — nunca versionar (cache local do stats.js; a fonte
# de verdade durável é o Neon e o app recria estes arquivos sozinho).
# "/pixels/" é ancorado na raiz: só o cache local, NÃO a UI em
# dashboard/components/pixels nem a rota dashboard/app/(dashboard)/pixels.
data/
/pixels/
*.tsbuildinfo
.vercel
